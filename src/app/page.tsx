'use client';

import { WorkbenchLayout } from '@/components/WorkbenchLayout';

export default function Home() {
  return (
    <main className="h-screen w-screen bg-slate-900 text-slate-50">
      <WorkbenchLayout />
    </main>
  );
}
